# Copie este arquivo para `.env` e preencha com as suas credenciais do Supabase.
# Estas chaves são PÚBLICAS (prefixo VITE_) — não coloque segredos aqui.
# As credenciais reais vêm do seu projeto Supabase (Settings > API).

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
